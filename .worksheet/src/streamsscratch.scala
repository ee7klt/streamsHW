import streams.Bloxorz._

object streamsscratch {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(93); 
  println("Welcome to the Scala worksheet");$skip(23); 
  val p = new Pos(2,3);System.out.println("""p  : <error> = """ + $show(p ))}
  
}
